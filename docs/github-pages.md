# GitHub Pages

Project URL:

```text
https://aifreedomtrustfederation.github.io/capital-city-provisions/
```

Recommended setup for this Next.js app:

- Keep the production application deployment separate when needed.
- For GitHub Pages, use a static export build with the `/capital-city-provisions/` base path.
- Enable GitHub Pages using GitHub Actions after the static export workflow is added and tested.

Federation map:

```text
https://aifreedomtrustfederation.github.io/www.aifreedomtrust.com/pages.html
```
