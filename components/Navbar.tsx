'use client';
import {useState} from 'react';
const links=[['Menu','/menu'],['Boxes','/freezer-boxes'],['How It Works','/how-delivery-works'],['Delivery','/delivery-map'],['Giveaway','/giveaway'],['Contact','/contact']];
export default function Navbar(){const [open,setOpen]=useState(false);return <nav className="nav" aria-label="Main navigation"><a className="brand" href="/">Capital City Provisions</a><button className="menu-button" onClick={()=>setOpen(!open)} aria-expanded={open} aria-controls="main-navigation">Menu</button><div id="main-navigation" className={open?'nav-links open':'nav-links'}>{links.map(([label,href])=><a key={href} href={href} onClick={()=>setOpen(false)}>{label}</a>)}</div></nav>}